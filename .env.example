# Optimizely Personal Access Token
# Get your token from: https://app.optimizely.com (Profile → API Access)
OPTIMIZELY_TOKEN=your_personal_access_token_here

# Optional: Custom API base URL (defaults to https://api.optimizely.com/v2)
# OPTIMIZELY_BASE_URL=https://api.optimizely.com/v2

# Optional: Request timeout in milliseconds (defaults to 30000)
# OPTIMIZELY_TIMEOUT=30000
